# Variables que consume docker-compose.yml. Cámbialas si los puertos
# chocan con otro proyecto que tengas levantado.

# Servidor de desarrollo (ng serve, con recarga en vivo).
MURAL_PORT=4200

# Mural compilado servido por nginx. Es la URL que se abre en el televisor.
TV_PORT=8080
